<% if user_signed_in? %>
  <%= form_with url: user_search_path, method: :get, local: true do |f| %>
    <div class="col-auto">
      <div class="input-group">
        <%= f.text_field :content, class: "form-control" %>
        <%= f.select :model, options_for_select({ "ユーザー" => "user", "投稿" => "post" }), class: "form-control"  %>
        <div class="input-group-text">
          <%= button_tag :type => "submit", :class => "btn btn-default p-0" do %>
          <i class="fa fa-search"></i>
          <% end %>
          <%#= f.submit '検索', class: "form-submit btn btn-default p-0" %>
        </div>
      </div>
    </div>
          
      <%#= f.text_field :content %>  
      <%#= f.select :model, options_for_select({ "ユーザー" => "user", "投稿" => "post" }) %>
      <%#= f.select :method, options_for_select({ "完全一致" => "perfect", "前方一致" => "forward", "後方一致" => "backward", "部分一致" => "partial" }) %>
      <%#= f.submit '検索', class: "form-submit" %>
  <% end %>
<% end %>